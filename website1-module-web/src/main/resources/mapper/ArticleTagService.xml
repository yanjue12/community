<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fzg.service.ArticleTagService">


    <select id="listTagNamesByArticleId" resultType="java.lang.String">
        select distinct att.name
        from article_tag_relation atr
        join article_tag  att on atr.tag_id = att.id
        where atr.article_id = #{id}
    </select>
</mapper>